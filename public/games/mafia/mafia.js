const socket=io(),app=document.getElementById('app'),qs=new URLSearchParams(location.search);
let code=(qs.get('code')||'').toUpperCase(),token=qs.get('sessionToken')||'',name=qs.get('name')||'',G=null,me=null,selected=null,roleTimer=null,roleCycle=null,dayTimer=null;
const roles={mafia:['🔪','المافيا','/games/mafia/assets/mafia.svg'],detective:['🕵️','المحقق','/games/mafia/assets/detective.svg'],doctor:['🩺','الطبيب','/games/mafia/assets/doctor.svg'],jester:['🤡','المهرج','/games/mafia/assets/jester.svg'],citizen:['👤','المواطن','/games/mafia/assets/citizen.svg']};
const roleKeys=Object.keys(roles);
const R=r=>roles[r]||['🎭','اللاعب','/games/mafia/assets/citizen.svg'];
const esc=x=>String(x??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function clearTimers(){clearInterval(roleTimer);clearInterval(roleCycle);clearInterval(dayTimer)}
function ps(){return(G.players||[]).map(p=>`<div class="player ${p.alive===false?'dead':''}"><b>${esc(p.name)}</b><div class="muted">${p.alive===false?'💀 ميت':p.online===false?'🟡 غير متصل':'🟢 حي'}</div></div>`).join('')}
function timer(){clearInterval(window.ti);window.ti=setInterval(()=>{const e=document.getElementById('timer');if(e&&G?.phaseUntil)e.textContent=Math.max(0,Math.ceil((G.phaseUntil-Date.now())/1000))+' ث'},200)}
function post(t){app.innerHTML=`<div class="card"><div class="notice">${t}</div></div>`}
function setup(){let c=G.config||{};app.innerHTML=`<div class="card setup-card"><div class="phase">🎭 مافيا</div><h1>إعداد القيم</h1><div class="muted">كود الغرفة: <b>${esc(G.code)}</b></div><h3>اللاعبون ${G.players.length}/${c.maxPlayers}</h3><div class="players">${ps()}</div><div class="grid">${[['maxPlayers','عدد اللاعبين',c.maxPlayers||8,4,20],['mafiaCount','🔪 المافيا',c.mafiaCount||2,1,20],['detectiveCount','🕵️ المحققون',c.detectiveCount||1,0,20],['doctorCount','🩺 الأطباء',c.doctorCount||1,0,20],['jesterCount','🤡 المهرجون',c.jesterCount||0,0,20],['nightSec','الليل (ثانية)',c.nightSec||30,10,120],['discussionSec','النقاش (ثانية)',c.discussionSec||90,15,300],['voteSec','التصويت (ثانية)',c.voteSec||30,10,120]].map(x=>`<div class="field"><label>${x[1]}</label><input class="input" id="${x[0]}" type="number" min="${x[3]}" max="${x[4]}" value="${x[2]}"></div>`).join('')}</div><div class="grid"><div class="field"><label>كشف الدور عند الموت</label><select class="select" id="revealRole"><option value="1" ${c.revealRole?'selected':''}>نعم</option><option value="0" ${!c.revealRole?'selected':''}>لا</option></select></div><div class="field"><label>التعادل</label><select class="select" id="tieMode"><option value="none" ${c.tieMode!=='revote'?'selected':''}>لا يخرج أحد</option><option value="revote" ${c.tieMode==='revote'?'selected':''}>إعادة التصويت</option></select></div></div><button class="btn primary" onclick="saveC()">💾 حفظ الإعدادات</button><button class="btn good" style="margin-top:10px" onclick="start()" ${G.players.length!==c.maxPlayers?'disabled':''}>🎭 بدء اللعبة</button></div>`}
window.saveC=()=>{let n=id=>+document.getElementById(id).value;socket.emit('mafia:config',{maxPlayers:n('maxPlayers'),mafiaCount:n('mafiaCount'),detectiveCount:n('detectiveCount'),doctorCount:n('doctorCount'),jesterCount:n('jesterCount'),nightSec:n('nightSec'),discussionSec:n('discussionSec'),voteSec:n('voteSec'),revealRole:document.getElementById('revealRole').value==='1',tieMode:document.getElementById('tieMode').value})};
window.start=()=>socket.emit('mafia:start');
function roleReveal(){clearTimers();const remaining=Math.max(0,(G.phaseUntil||0)-Date.now()),elapsed=Math.max(0,8500-remaining),final=R(G.myRole);app.innerHTML=`<div class="role-screen"><div class="role-stage" id="roleStage"></div><div class="role-caption" id="roleCaption">استعد...</div><div class="role-final" id="roleFinal"></div></div>`;const stage=document.getElementById('roleStage'),cap=document.getElementById('roleCaption'),rf=document.getElementById('roleFinal');function card(r,extra=''){const x=R(r);return `<div class="roulette-card ${extra}"><img src="${x[2]}" alt=""><div>${x[0]} ${x[1]}</div></div>`}if(elapsed<3000){let left=Math.ceil((3000-elapsed)/1000);cap.textContent='سيظهر دورك خلال';stage.innerHTML=`<div class="countdown-big">${left}</div>`;roleTimer=setInterval(()=>{const rem=Math.max(0,3000-(Date.now()-(G.phaseUntil-8500)));const n=Math.ceil(rem/1000);if(n>0)document.getElementById('roleStage').innerHTML=`<div class="countdown-big">${n}</div>`},100)}else if(elapsed<7000){cap.textContent='جاري اختيار دورك...';let i=Math.floor((elapsed-3000)/180)%roleKeys.length;stage.innerHTML=card(roleKeys[i]);roleCycle=setInterval(()=>{i=(i+1)%roleKeys.length;stage.innerHTML=card(roleKeys[i])},180)}else{cap.textContent='هذا دورك';rf.innerHTML=card(G.myRole,'final-role')}const showFinal=()=>{clearInterval(roleTimer);clearInterval(roleCycle);document.getElementById('roleStage').innerHTML=card(G.myRole,'final-role');document.getElementById('roleCaption').textContent='هذا دورك';};if(elapsed<7000)setTimeout(showFinal,Math.max(0,7000-elapsed));else showFinal()}
function targetButtons(){const marks={};if(G.mafiaSelections)Object.values(G.mafiaSelections).forEach(x=>{marks[x.targetId]=(marks[x.targetId]||[]).concat(x.playerName||'مافيا')});return(G.players||[]).filter(p=>p.alive!==false&&p.id!==me).map(p=>{const m=(marks[p.id]||[]).map(n=>`<small>🔪 ${esc(n)} اختاره</small>`).join('');return `<button class="btn target ${selected===p.id?'selected':''}" onclick="pick('${p.id}')"><b>${esc(p.name)}</b>${m}</button>`}).join('')}
window.pick=id=>{selected=id;render()};
function night(){clearTimers();if(G.myAlive===false)return deadNight();let r=R(G.myRole),a=G.myAction||{},isM=G.myRole==='mafia',isD=G.myRole==='doctor',isI=G.myRole==='detective';if(G.myRole==='citizen'||G.myRole==='jester'){app.innerHTML=`<div class="card night-card"><div class="phase">🌙 الليل</div><div class="night-icon">🌙</div><h1>${G.myRole==='citizen'?'المواطن':'المهرج'}</h1><p class="muted">لا يمكنك القيام بأي حركة ليلية. شاهد فقط حتى يحل النهار.</p><div class="timer" id="timer"></div><div class="notice">🤫 الجميع في الليل الآن... انتظر</div></div>`;timer();return}let chosen=a?.targetId||selected,confirmed=a?.submitted===true;let title=isM?'🔪 اختر ضحية':isD?'🩺 اختر من تحميه':'🕵️ اختر من تريد التحقيق معه';let status='';if(confirmed){let tn=G.players.find(p=>p.id===a.targetId)?.name||'';status=isM?`<div class="success-box">🔪 تم اغتيال <b>${esc(tn)}</b><br><span>بانتظار حلول النهار...</span></div>`:isD?`<div class="success-box">🩺 تم حماية <b>${esc(tn)}</b> بنجاح<br><span>بانتظار حلول النهار...</span></div>`:`<div class="success-box">🕵️ لقد سألت عن <b>${esc(tn)}</b><br><span>جاري كشف الحقيقة...</span></div>`}else if(isI&&G.investigationResult){let x=R(G.investigationResult);status=`<div class="investigation-reveal"><div class="muted">نتيجة التحقيق</div>${rouletteTarget(x)}</div>`}app.innerHTML=`<div class="card night-card"><div class="phase">🌙 الليل بدأ</div><h1>${title}</h1><div class="timer" id="timer"></div>${status}${!confirmed?`<div class="target-grid">${targetButtons()}</div><button class="btn primary confirm" onclick="nightSend()" ${chosen?'':'disabled'}>✅ تأكيد الاختيار</button>`:''}${isM&&G.mafiaSelections?.length?`<div class="notice mafia-note">اختيارات المافيا تظهر هنا لتتفقوا قبل التأكيد.</div>`:''}</div>`;selected=chosen||null;timer();if(isI&&G.investigationResult&&confirmed)animateInvestigation(G.investigationResult,a.targetId)}
function rouletteTarget(x){return `<div class="investigation-card"><img src="${R(x)[2]}"><b>${R(x)[0]} ${R(x)[1]}</b></div>`}
function animateInvestigation(role,targetId){if(window.invDone===targetId)return;window.invDone=targetId;const box=document.querySelector('.investigation-reveal');if(!box)return;let i=0;box.innerHTML='<div class="muted">الروليت تعمل...</div><div class="investigation-card"><img id="invImg"><b id="invName"></b></div>';const im=document.getElementById('invImg'),nm=document.getElementById('invName');roleCycle=setInterval(()=>{const r=R(roleKeys[i++%roleKeys.length]);im.src=r[2];nm.textContent=r[0]+' '+r[1]},180);setTimeout(()=>{clearInterval(roleCycle);box.innerHTML=`<div class="muted">الروليت توقفت على:</div>${rouletteTarget(role)}<div class="wait-text">بانتظار حلول النهار...</div>`},2600)}
function nightSend(){if(!selected)return;const isM=G.myRole==='mafia';socket.emit('mafia:nightAction',{targetId:selected,confirm:true});}
function deadNight(){app.innerHTML=`<div class="card ghost"><div class="ghost-mark">👻</div><h1>أنت ميت</h1><p>لا يمكنك القيام بدورك، أنت ميت... الله يرحمك.</p><div class="timer" id="timer"></div><div class="notice">👻 يمكنك مشاهدة اللعبة فقط، دون معرفة الأدوار السرية.</div></div>`;timer()}
function discussion(){clearTimers();let chat=(G.chat||[]).map(x=>`<div class="chat-line"><b>${esc(x.name)}</b><span>${esc(x.text)}</span></div>`).join('');let nr=G.lastNightResult?.victimName?`<div class="day-reveal" id="dayReveal"><div class="reveal-step">حلّ النهار...</div></div>`:G.lastNightResult?.saved?`<div class="day-reveal" id="dayReveal"><div class="reveal-step">حلّ النهار...</div></div>`:`<div class="day-reveal" id="dayReveal"><div class="reveal-step">حلّ النهار...</div></div>`;app.innerHTML=`<div class="card day-card"><div class="phase">☀️ النهار</div><h1>ماذا حدث هذه الليلة؟</h1><div class="timer" id="timer"></div>${nr}<div class="chat-box">${chat||'<div class="muted">لا توجد رسائل بعد.</div>'}</div>${G.myAlive!==false?`<div class="field"><input id="chatText" class="input" maxlength="180" placeholder="اكتب رسالتك..."></div><button class="btn primary" onclick="chatSend()">إرسال</button>`:'<div class="notice">👻 أنت ميت، يمكنك المشاهدة فقط.</div>'}<div class="players">${ps()}</div></div>`;animateDay();timer()}
function animateDay(){const el=document.getElementById('dayReveal');if(!el)return;const result=G.lastNightResult||{};setTimeout(()=>{el.innerHTML='<div class="reveal-step">هناك خبر سيئ...</div>'},900);setTimeout(()=>{if(result.victimName)el.innerHTML=`<div class="reveal-big death">💀 تم اغتيال <b>${esc(result.victimName)}</b></div>`;else if(result.saved)el.innerHTML='<div class="reveal-big saved">🩺 الطبيب نجح بالحماية</div>';else el.innerHTML='<div class="reveal-big">🌙 مرت الليلة دون ضحية</div>'},2300)}
window.chatSend=()=>{let e=document.getElementById('chatText');if(e?.value.trim()){socket.emit('mafia:chat',{text:e.value.trim()});e.value=''}};
function vote(){clearTimers();if(G.myAlive===false)return deadVote();if(G.myRole==='jester')return noVote();let a=G.myAction||{};app.innerHTML=`<div class="card"><div class="phase">🗳️ التصويت</div><h1>من المشتبه به؟</h1><div class="timer" id="timer"></div>${a.submitted?'<div class="success-box">✅ تم تسجيل تصويتك<br><span>انتظر بقية اللاعبين...</span></div>':`<div class="target-grid">${targetButtons()}</div><button class="btn primary" onclick="voteSend()" ${selected?'':'disabled'}>🗳️ تأكيد التصويت</button>`}<div class="players">${ps()}</div></div>`;timer()}
function noVote(){app.innerHTML=`<div class="card ghost"><div class="ghost-mark">🤡</div><h1>المهرج لا يصوّت</h1><p>دورك هو البقاء ومشاهدة التصويت.</p><div class="timer" id="timer"></div></div>`;timer()}
function deadVote(){app.innerHTML=`<div class="card ghost"><div class="ghost-mark">👻</div><h1>أنت ميت</h1><p>لا يمكنك التصويت. شاهد ما يحدث فقط.</p><div class="timer" id="timer"></div></div>`;timer()}
window.voteSend=()=>{if(!selected)return;socket.emit('mafia:vote',{targetId:selected});selected=null};
function voteResult(){let v=G.voteResult||{},rows=(v.rows||[]).map(x=>`<div class="player"><b>${esc(x.name)}</b><span class="voteCount">${x.count} صوت</span></div>`).join('');let text=v.tie?'⚖️ تعادل! لم يتم إخراج أي لاعب.':v.eliminatedName?`🚪 تم إخراج <b>${esc(v.eliminatedName)}</b>`:'لا يوجد إخراج';app.innerHTML=`<div class="card"><div class="phase">📊 نتيجة التصويت</div><h1>${text}</h1><div class="timer" id="timer"></div><div class="players">${rows}</div></div>`;timer()}
function finished(){clearTimers();let w=G.winnerText||'';let cls=w.includes('المافيا')?'mafia-win':w.includes('المواطنون')?'citizen-win':'jester-win';let text=w.includes('المافيا')?'المافيا فازت':w.includes('المواطنون')?'المواطنون فازوا':'المهرج فاز';app.innerHTML=`<div class="winner-screen ${cls}"><div class="winner-word">${text}</div></div>`}
function game(){if(G.phase==='role')return roleReveal();if(G.phase==='night')return night();if(G.phase==='discussion')return discussion();if(G.phase==='vote')return vote();if(G.phase==='voteResult')return voteResult();if(G.phase==='finished')return finished();post('جاري الانتقال…')}
function render(){if(!G)return post('جاري الاتصال…');if(G.phase==='lobby')return setup();game()}
let joinSent=false;function connectMafia(){if(!code)return post('لا يوجد كود غرفة.');if(joinSent)return;joinSent=true;if(token)socket.emit('room:resume',{code,sessionToken:token,name});else socket.emit('room:join',{code,name:name||'لاعب'})}
socket.on('connect',connectMafia);socket.on('joined',x=>{code=x.code;me=x.playerId;token=x.sessionToken;try{localStorage.setItem('mafia.session',JSON.stringify({code,token,name:name||'لاعب',updatedAt:Date.now()}))}catch(e){}socket.emit('room:sync',{code})});socket.on('state',s=>{G=s;render()});socket.on('errorMsg',m=>{joinSent=false;post('⚠️ '+esc(m))});socket.on('resumeFailed',x=>{joinSent=false;post('⚠️ '+esc(x.message||'تعذر استعادة الغرفة'))});if(code&&socket.connected)connectMafia();
